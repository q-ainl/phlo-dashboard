<?php
# Node-lokale bootstrap - kopieer naar www/app.php en vul je eigen host/paden in.
# Niet in git: elke node heeft z'n eigen www/app.php (zie .gitignore).
require('/srv/control/phlo/phlo.php');
phlo_app (
	id: 'Dashboard',
	host: 'dashboard.example.tld',   # jouw dashboard-host
	auth: false,
	build: false,                    # dev: true, prod: false
	debug: false,                    # dev: true, prod: false
	dashboard: 'phlo',
	app: '/srv/control/dashboard/',  # pad per node aanpassen (sommige nodes wijken af)
	websocket: 3001,
	files: '/srv/control/files/',
	images: '/srv/control/files/images/',
	thumbs: '/srv/control/files/thumbs/',
);
